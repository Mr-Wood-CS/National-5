# Videoblogger: SELECT and WHERE

Use the Videoblogger database to practise `SELECT`, `FROM` and `WHERE` queries.

[:material-database: Open the SQL workspace](https://onecompiler.com/sqlite/44t39rhad){: .inline-launch__link target="_blank" rel="noopener" }

## Practice

You should practise:

- `SELECT ... FROM`
- `WHERE`
- comparison operators such as `=`, `<`, `>`, `<=` and `>=`

---

**Question 1**
Display the video name and video duration of the video called Microbits.

---

**Question 2**
Display the full names of all the Makeup experts.

==Credit: Greig Reid==
