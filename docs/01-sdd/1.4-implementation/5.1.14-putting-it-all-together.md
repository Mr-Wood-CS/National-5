# Putting It All Together

These tasks bring together the skills you have learned so far in Software Design and Development.

For each task, you should complete the development stages you have learned so far:

1. analyse the problem using an Input-Process-Output table
2. identify the data types and data structures needed
3. design the program using pseudocode
4. create either a structure diagram or a flowchart
5. implement the program in Python

Your Python programs should include:

- internal commentary
- variables
- arrays
- string, integer and real values
- input
- output
- number conversion using `int()` or `float()`
- calculations
- string concatenation or clear use of commas in `print()`

---

## Task 1: School Trip Calculator

A teacher wants a program to help calculate the cost of a school trip.

The program should ask the user to enter:

- the pupil's name
- the destination
- the number of pupils going on the trip
- the entry cost per pupil
- the total bus cost

The program should store the destination and pupil name using variables.

The program should store the following labels in an array:

```python
costLabels = ["Entry total", "Bus cost", "Total cost"]
```

The program should calculate:

- the total entry cost
- the total trip cost
- the cost per pupil including the bus cost

The program should display a clear trip summary showing:

- the pupil's name
- the destination
- each cost label from the array
- the total entry cost
- the bus cost
- the total trip cost
- the final cost per pupil

### Analysis

Complete an Input-Process-Output table.

| Input | Process | Output |
|-------|---------|--------|
| | | |
| | | |
| | | |

### Data Types and Structures

Complete the table below.

| Data Item | Data Type | Data Structure | Reason |
|-----------|-----------|----------------|--------|
| Pupil name | | | |
| Destination | | | |
| Number of pupils | | | |
| Entry cost per pupil | | | |
| Total bus cost | | | |
| Cost labels | | | |

### Design

Write pseudocode for the program.

Create either a structure diagram or a flowchart for the program.

### Implementation

Create the program in Python.

Remember to:

- use comments to explain the main sections of your program
- convert number inputs using `int()` or `float()`
- use meaningful variable names
- use the array when displaying the cost labels

### Example Run

Use the values below to check what your program should calculate.

| Example Input | Expected Output |
|---------------|-----------------|
| Pupil name: `Ava`<br>Destination: `Science Centre`<br>Number of pupils: `30`<br>Entry cost per pupil: `6.50`<br>Bus cost: `180` | Entry total: `195.0`<br>Total cost: `375.0`<br>Cost per pupil: `12.5` |

---

## Task 2: Class Test Summary

A teacher wants a program that stores the names and scores of three pupils in a class test.

The program should ask the user to enter:

- the name of pupil 1
- the score for pupil 1
- the name of pupil 2
- the score for pupil 2
- the name of pupil 3
- the score for pupil 3

The program should store:

- the pupil names in an array
- the test scores in an array

The program should calculate:

- the total score
- the average score

The program should display:

- all three pupil names and scores
- the total score
- the average score

### Analysis

Complete an Input-Process-Output table.

| Input | Process | Output |
|-------|---------|--------|
| | | |
| | | |
| | | |

### Data Types and Structures

Complete the table below.

| Data Item | Data Type | Data Structure | Reason |
|-----------|-----------|----------------|--------|
| Pupil names | | | |
| Test scores | | | |
| Total score | | | |
| Average score | | | |

### Design

Write pseudocode for the program.

Create either a structure diagram or a flowchart for the program.

### Implementation

Create the program in Python.

Remember to:

- create an empty array for the pupil names
- create an empty array for the scores
- use `append()` to add each name and score
- convert each score to an integer
- use array indexes when displaying each pupil and score

### Example Run

Use the values below to check what your program should calculate.

| Example Input | Expected Output |
|---------------|-----------------|
| Pupil 1: `Mia`, Score: `12`<br>Pupil 2: `Noah`, Score: `15`<br>Pupil 3: `Leo`, Score: `18` | Total score: `45`<br>Average score: `15.0` |

---

## Task 3: Bake Sale Takings

A charity group wants a program to calculate how much money was made at a bake sale.

The program should use these arrays:

```python
itemNames = ["Cupcakes", "Brownies", "Cookies"]
itemPrices = [1.50, 2.00, 0.75]
```

The program should ask the user to enter:

- how many cupcakes were sold
- how many brownies were sold
- how many cookies were sold
- the cost of ingredients

The program should calculate:

- the money made from cupcakes
- the money made from brownies
- the money made from cookies
- the total money made
- the profit

The program should display a clear bake sale summary showing:

- each item name from the array
- the money made from each item
- the total money made
- the ingredient cost
- the profit

### Analysis

Complete an Input-Process-Output table.

| Input | Process | Output |
|-------|---------|--------|
| | | |
| | | |
| | | |

### Data Types and Structures

Complete the table below.

| Data Item | Data Type | Data Structure | Reason |
|-----------|-----------|----------------|--------|
| Item names | | | |
| Item prices | | | |
| Number sold | | | |
| Ingredient cost | | | |
| Total money made | | | |
| Profit | | | |

### Design

Write pseudocode for the program.

Create either a structure diagram or a flowchart for the program.

### Implementation

Create the program in Python.

Remember to:

- use the arrays given in the task
- convert number inputs using `int()` or `float()`
- use calculations to work out each total
- display the output clearly
- include comments to explain the main sections of your program

### Example Run

Use the values below to check what your program should calculate.

| Example Input | Expected Output |
|---------------|-----------------|
| Cupcakes sold: `20`<br>Brownies sold: `15`<br>Cookies sold: `40`<br>Ingredient cost: `18.50` | Cupcake money: `30.0`<br>Brownie money: `30.0`<br>Cookie money: `30.0`<br>Total money made: `90.0`<br>Profit: `71.5` |
