# If/Else Statements

An `if/else` statement chooses between two paths. One path runs when the condition is true and the other runs when it is false.

## Designing an If/Else Statement

This pseudocode describes a decision with two possible paths:

```pseudocode title="Driving Age Check Design" linenums="1"
1 age = integer input
2 IF age >= 17 THEN
3     SEND "You are old enough to drive" TO DISPLAY
4 ELSE
5     SEND "You are not old enough to drive yet" TO DISPLAY
6 END IF
```

The indentation and `END IF` show which instructions belong to the selection.

## Implementing an If/Else Statement

This program checks whether someone is old enough to drive.

```python title="Driving Age Check" linenums="1"
age = int(input("Please enter your age: "))

if age >= 17:
    print("You are old enough to drive")
else:
    print("You are not old enough to drive yet")
```

- When `age >= 17` is true, the first message is displayed.
- Otherwise, the condition is false and the message inside `else` is displayed.

Only one branch can run. The two outcomes are **mutually exclusive**.

## Structure of an If/Else Statement

```python
if condition:
    statement_when_true
else:
    statement_when_false
```

Both the `if` and `else` lines end with a colon. The statements belonging to each branch must be indented.

!!! note

    `else` does not have its own condition. It means "when the `if` condition is false".

## Another Example

```python title="Even or Odd"
number = int(input("Enter a whole number: "))

if number % 2 == 0:
    print("The number is even")
else:
    print("The number is odd")
```

## Check Your Knowledge

!!! question "Programming Task"

    Create a Python program that asks the user to enter a test mark.

    The program should display:

    - `Pass` when the mark is 50 or more
    - `Fail` otherwise

    Test the program using the marks `49`, `50`, and `51`.
