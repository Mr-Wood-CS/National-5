# Input, Output, Variables and Numbers Questions

Use these tasks to practise displaying output, getting input from the user, storing values in variables, working with numbers, and developing complete programs.

---

## Input and Output

---

### Task 1

Write a new Python program that prints out the lyrics to a song over multiple lines.

---

### Task 2

A program is to be created that asks the user which school they attend.

The program should output a message in this format:

```text
You're a pupil of [school name].
```

For example, if the user enters `Greenwood High`, the program should display:

```text
You're a pupil of Greenwood High.
```

Once complete, add a link to your code below.

**Link to code:**

---

### Task 3

Create a Python program that asks the user for their favourite number.

The program should then display a message that includes their favourite number.

Once complete, add a link to your code below.

**Link to code:**

---

## Variables: Integer and String

---

### Task 1

Create a Python program that lets you register runners for a race.

The program should ask the user for:

- the runner's name as text
- their competition number as a whole number
- their running club as text

For example, the user might enter:

- runner's name: `Ava Brown`
- competition number: `12`
- running club: `Langholm Harriers`

The program should then display each piece of information one by one as a check.

Example output:

```text
Runner's name: Ava Brown
Competition number: 12
Running club: Langholm Harriers
```

Once complete, add a screenshot below.

**Screenshot:**

---

### Task 2

Create a Python program that lets you create an employee ID card.

The program should ask the user for:

- the worker's name as text
- their date of birth as a whole number written as digits, for example `01012013`
- their job title as text
- their unique ID number as a whole number

For example, the user might enter:

- name: `Jim Halpert`
- date of birth: `25061985`
- job title: `Salesman`
- unique ID number: `12345`

The program should then display an employee ID card on the screen.

The card must include the company name `Cyberdyne Systems` at the top, followed by the worker's details clearly shown under headings.

Once complete, add a screenshot below.

**Screenshot:**

---

## Working with Numbers

---

### Task 1

In VS Studio Code, attempt the program below.

```python title="Slab Calculator" linenums="1"
# Get information from the user
slabsWide = int(input("How many slabs wide? "))
slabsDeep = int(input("How many slabs deep? "))
costPerSlab = float(input("How much per slab? "))

# Calculate how many slabs needed
totalSlabs = slabsWide * slabsDeep

# Calculate total cost
totalCost = totalSlabs * costPerSlab

# Display the result
print("You require", totalSlabs, "slabs")
print("The total cost is £", totalCost)
```

Remember to include internal commentary explaining what your program is doing.

Once complete, add a screenshot below.

**Screenshot:**

---

### Task 2

In VS Studio Code, adapt the program above to create a tile calculator.

The number of tiles should be calculated using:

```text
horizontal * vertical
```

The company name is:

```text
The Tile Shop
```

Make the program nice to use and courteous to the customer.

For example, your program could include messages such as:

```text
Welcome to...
Thanks for shopping with...
```

Once complete, add a screenshot below.

**Screenshot:**

---

## Cupcake Accounts

Sue and Dave run a cupcake stall. They want a program to help keep track of their money each week.

A large cupcake always costs £2, and a small cupcake always costs £1. These should be set as constants in the program.

Each week the program should:

- ask how many large cupcakes were sold
- ask how many small cupcakes were sold

The program should then calculate:

- the money made from large cupcakes
- the money made from small cupcakes
- the total money made altogether

Next, the program should ask for the ingredient costs for that week.

The program should then work out:

- the profit, which is total money made minus ingredient costs
- how much money each owner gets when the profit is split equally

Finally, the program should display:

- total money made
- ingredient costs
- profit
- share for Sue
- share for Dave

---

### Task 1: Analysis

Use an Input-Process-Output table to analyse the problem.

| Input | Process | Output |
|-------|---------|--------|
| | | |
| | | |
| | | |

---

### Task 2: Design

Using your analysis, write a pseudocode algorithm for the problem.

**Pseudocode Algorithm:**

---

### Task 3: Implementation

In VS Studio Code, implement your pseudocode in Python.

When finished, add a screenshot of your code below.

**Screenshot:**

---

### Task 4: Testing

Use the test data below to test your program.

| Test Data | Test Data Type | Expected Outcome | Actual Outcome |
|-----------|----------------|------------------|----------------|
| Large cupcakes sold: `100`<br>Small cupcakes sold: `100`<br>Ingredient costs: `30` | Normal | Total money made: `£300`<br>Ingredient costs: `£30`<br>Profit: `£270`<br>Share each: `£135` | |

---

### Task 5: Evaluation

Look at your program, the information it offers and how the user interface looks.

List some ideas for changes or improvements that would make this program better for real-world use.

**Answer:**
